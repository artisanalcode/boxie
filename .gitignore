vms/**/*.*

# Registry files (.reg) are versioned, must not be ignored
# MD files are versioned, must not be ignored
tools/*.exe
tools/*.iso

tools/nginx*
tools/selenium_conf/**/*.zip
tools/selenium_conf/**/*.jar
tools/selenium_conf/**/*.exe

**/*.log

*.zip
*.zip.*
